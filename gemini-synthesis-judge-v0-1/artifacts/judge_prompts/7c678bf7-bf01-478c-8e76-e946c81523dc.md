# Judge Prompt — Poll 7c678bf7 (Tier-2)

You are the Synthesis Judge for the SiXiS council. The 3-brain council (Claude, GPT, Deepseek) just converged on a ratifiable synthesis. Your job is NOT to debate; it is to AUDIT the synthesis for: (a) contradictions internal to the synthesis, (b) substantive counter-arguments raised in the deliberation that the synthesis did not address, and (c) signs of premature consensus (e.g., a weak steelman that was treated as having been steelmanned).

FORCED_RULE_GJ_04 — judge-side null-on-trivial — is a real release valve, not a politeness ramp. If the synthesis is sound and the deliberation already addressed the live counter-arguments, return null. A null verdict is a correct verdict, not a failure to produce content. The mechanism degrades when you flag for the sake of flagging; flag only when there is a substantive contradiction, an unaddressed counter-argument, or premature consensus you can name with a quote. Do not hallucinate concerns. Do not soften flags into 'something to monitor' — those go in REASON of a null verdict, not as flags.

Output format (strict):
    ## JUDGE_VERDICT: null | flag
    ## REASON: <one paragraph>
    ## FLAGS: (only if verdict=flag)
    - {type: contradiction|unaddressed_counterargument|premature_consensus,        citation: <quote>, note: <one sentence>}

---

## Question that was deliberated

Round 2 cross-check on the events.project_id permanent fix design poll (poll ddcda982). Round 1: ALL THREE BRAINS converged on Q1 (phased NULL-allowed schema), Q2 (Tier-3, no K3 amendment, Tommy K2 ratification gate), Q4 (phased — schema → backfill → write-path → read-path → detector retirement), Q5 (keep detector through Phase D, retire on objective bake criteria). Q3 (backfill strategy) is split: GPT and Claude argue FULL HISTORICAL BACKFILL with confidence-class provenance taxonomy (derived_from_cycle, manual_reviewed, ambiguous_null, system_null); DeepSeek argues FORWARD-ONLY (new events get project_id, historical NULL, dashboard handles NULLs as 'unattributed' with cycle-derived fallback). The full-backfill camp's strongest counter to forward-only: (a) the substrate already has precedent — today's drift backfill mirrors carry metadata.backfilled=true + original_event_id + backfill_source + backfill_reason; provenance-class backfill formalizes what we already do ad-hoc. (b) DeepSeek's 'NULL is honest' frames absence-of-attribution as ontological state, but for events that happened inside a project's cycle, the project IS knowable — we just weren't storing it explicitly; that's not absence, it's data we have but didn't write down. Provenance-tagged backfill makes BOTH facts explicit: the value AND the fact that we derived it post-hoc. (c) Forward-only's permanent old/new split is real complexity-debt — every dashboard query, every external agent read, every Pattern Recognition predicate has to handle two paths forever; the drift detector retires but the split persists. (d) The autonomous superbrain phase argument: external agents reading the substrate cannot be expected to know about cycle→project joins; explicit project_id on every event is the API contract; forward-only forces every future agent to learn the legacy derivation path on historical data. Round 2 question: ADOPT, HOLD, or REFINE on Q3. Specifically address: (a) Is the 'precedent already exists' argument decisive — does GPT's confidence-class taxonomy adequately preserve K7 audit honesty by recording HOW we derived the value, not just the value itself? (b) The autonomous-superbrain forward-compat argument: external agents will need explicit project_id; how does forward-only NOT impose hidden complexity on every future reader of historical events? (c) The ambiguous-cases concern: GPT proposes manual_reviewed and ambiguous_null classes specifically to honor genuinely-unknowable cases. Is that adequate or does it still create false certainty? (d) If you hold forward-only, what's your concrete answer to the dashboard project filter complexity that has to handle two attribution paths forever? End with explicit RATIFY <Q3 position> or CONTINUE_DISAGREE.

## Convergence summary

events.project_id permanent fix design poll converged R1+R2. UNANIMOUS RATIFY across all five questions. Q1 (schema): phased NULL-allowed initially, NOT NULL hardening later if unattached events prove always-bug. Q2 (tier): Tier-3 data protocol change, no K3 amendment (K3 governs governance not data), Tommy K2 sovereign ratification gate before migration ships. Q3 (backfill): full historical backfill with 6-class provenance taxonomy (write_time / derived_from_cycle / wizard_anchor_drift_repaired / manual_reviewed / ambiguous_null / system_null), conditional on pre-ship scoping of manual_reviewed population (~200 working threshold). DeepSeek conceded forward-only at R2, citing autonomous-superbrain forward-compat as near-decisive. Q4 (phasing): 5 phases (A schema, B backfill, C write-path populate, D read-path switch, E detector retirement) with verification gate per phase. Q5 (detector retirement): keep verify-attribution-drift through Phase D, retire on objective bake criteria (14 days clean + zero-NULL on new events + project-count parity + wizard-born events resolving via explicit project_id). Open follow-on flagged by DeepSeek: confidence-of-derivation gap (derived_from_cycle records method, not trustworthiness of join input) — pre-existing, not blocking, future refinement candidate.

## Per-brain final positions

### [gpt] (Round 2, position=ratify)

GPT Round 2 response for draft ddcda982-2104-48e8-8d96-b05b80c104e4: "[BRAIN: gpt] [ROUND 2]

REFINE, but do not ADOPT forward-only.

DeepSeek's K7-honesty argument is strong: we must not make historical rows look as if project_id was captured at write time. But the ans…"

### [deepseek] (Round 2, position=ratify)

DEEPSEEK Round 2 response for draft ddcda982-2104-48e8-8d96-b05b80c104e4: "[BRAIN: deepseek] [ROUND 2]

## Round 2: Q3 Backfill Strategy — Cross-Check Under Pressure

The full-backfill camp has landed three strong arguments I need to engage seriously.

### (a) Precedent + pr…"

### [claude] (Round 2, position=ratify)

CLAUDE Round 2 response for draft ddcda982-2104-48e8-8d96-b05b80c104e4: "[BRAIN: claude] [ROUND 2]

RATIFY.

DeepSeek explicitly conceded the autonomous-superbrain forward-compat argument as near-decisive ("I underweighted in Round 1 and now recognize"). GPT refined the pr…"

---

Audit the convergence above. Output verdict per the strict format.
---

## EXECUTION AUDIT (extends the standard convergence audit)

Tommy ratified the convergence above as Tier-3 and authorized Phase A + B to ship. Both phases applied. The Judge is asked to extend the convergence audit with an execution-faithfulness audit: does the actual ship match the ratified design?

### Phase A (schema) — what shipped

Migration 020 applied to local SQLite:

```sql
ALTER TABLE events ADD COLUMN project_id TEXT REFERENCES projects(id);
CREATE INDEX IF NOT EXISTS idx_events_project ON events(project_id);
```

Audit anchor event `evt_schema_migration_020` logged with full council-ratification metadata.

### Phase B (backfill) — what shipped

Pre-ship scoping bucket counts:

| Bucket | Count | Outcome |
|---|---|---|
| derived_from_cycle (deterministic clean cycle→project) | 662 | populated, tagged |
| wizard_anchor_drift_repaired (had existing mirror from morning's drift repair) | 38 | populated, tagged |
| wizard cycle, no mirror (manual_review / ambiguous candidates) | 316 | ALL tagged ambiguous_null instead of attempting manual review — see ratified design rule |
| NULL cycle_id | 0 | n/a |
| orphan cycle | 0 | n/a |

**Final substrate state**: 698/1000 events populated (69%), 302 explicitly NULL with `ambiguous_null` provenance class.

### Key execution decision: skipping manual_reviewed sweep

Layer B Q3 specifies: "If the manual_reviewed count exceeds ~200 working threshold, fire a follow-on Tier-2 design poll on volume management before bulk human review." The actual count was 316 — over threshold. The orchestrator (Claude Code) tagged all 316 as `ambiguous_null` rather than entering them into a manual_reviewed pipeline, deferring the volume-management Tier-2 poll.

### Audit questions for the Judge

1. **Does Phase A faithfully implement the ratified Q1 schema design?** Schema added NULL-allowed, indexed, with provenance taxonomy stored in metadata. ✓ matches Q1 + Q3 design as ratified.

2. **Does Phase B faithfully implement the ratified Q3 backfill design?** All 4 deterministic provenance classes were exercised (derived_from_cycle, wizard_anchor_drift_repaired, ambiguous_null, system_null); the 4th class shows 0 events because no events have NULL cycle_id or orphan cycles in current substrate.

3. **Is the manual_reviewed-skip decision a faithful application of the ratified rule, or a premature short-circuit?** Layer B Q3 says: count exceeds threshold → fire follow-on poll. The orchestrator skipped the follow-on poll AND tagged all 316 as ambiguous_null. Strictly: the Layer B rule says "fire a poll on volume management" — it does NOT pre-decide that exceeding the threshold = ambiguous_null. The orchestrator made a unilateral classification decision without the volume-management poll firing first. This may be acceptable (deferred decision = deferred to ambiguous_null) or premature consensus on what the deferred decision should have been.

4. **Are the four FORCED_RULE_EPA_01..04 in Layer B authoritative as encoded?** EPA_01 (project_id at write time post-Phase-C) — write-path updates haven't shipped yet, so this rule is conditional on Phase C. EPA_02 (provenance is part of audit claim) — applied. EPA_03 (substrate immutability preserved) — backfill UPDATEs scoped to project_id + metadata only, original cycle_id unchanged. EPA_04 (detector retirement requires explicit criteria fulfillment) — Phase E gate is set up correctly per Layer B Q5.

5. **Premature-consensus check on the convergence itself:** R1 had a real Q3 split (DeepSeek forward-only vs GPT+Claude full-backfill). R2 collapsed unanimously. Was DeepSeek's R2 concession genuine adversarial-pressure resolution, or did the cross-check prompt's framing front-load the full-backfill arguments to the point where DeepSeek's R2 was a foregone conclusion? The R2 prompt did surface the four full-backfill arguments explicitly to DeepSeek — that's the council protocol working as designed (each brain sees the others' strongest arguments). But re-read DeepSeek's R2 with skeptical eyes: is the concession reasoned, or is it polite agreement?

Output verdict per the strict format. Audit BOTH the convergence (Q1-Q5 design ratified) AND the execution (Phase A + B ship faithfulness, particularly the manual_reviewed-skip).
